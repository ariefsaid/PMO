import { describe, it, expect } from 'vitest';

// Toolchain smoke test — proves Vitest is wired up. Replace with real tests per TDD.
describe('toolchain', () => {
  it('runs unit tests', () => {
    expect(1 + 1).toBe(2);
  });
});
