# op-get.sh COORDINATES for a FUTURE hosted test/staging env — NOT a secret, safe to commit.
# NOTE: today "test" = LOCAL Docker (`supabase start` / `supabase db reset`), which needs no
# 1Password secret. This file + scripts/db-push-test.sh are dormant forward-compat for when a
# separate HOSTED test/staging project is added. Until then, ignore them and test on local.
OP_TEST_ITEM=pmo-supabase-test
OP_TEST_VAULT=AS
OP_TEST_FIELD=db_url
