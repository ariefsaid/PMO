# Plan: External-system admin-connect layer (ClickUp + ERPNext, org-Admin self-serve)

> **Spec:** `docs/specs/external-admin-connect.spec.md` (signed — 23 `AC-EAC-###`, EARS `FR-EAC-###`/`NFR-EAC-###`).  
> **ADR:** `docs/adr/0059-external-admin-connect.md` (encodes OD-INT-1..5).  
> **Scope source:** `docs/plans/2026-07-13-clickup-admin-integration-flow.md` §5 (phased D1–D5), §6 (security), §11 (#315 alignment).  
> **Depends-on ADRs:** ADR-0055 (external adapters/SoT), ADR-0016 (`can()` UX + RLS authority), ADR-0019 (server-enforced privileged writes via security-definer RPC), ADR-0017 (repository seam), ADR-0018 (soft-archive), ADR-0057 (`verifyCallerJwt` local JWKS).  
> **Builds on (merged):** #315 ERPNext P2 → `supabase/migrations/0096_erpnext_seam_tables.sql` (`external_org_bindings` + `external_command_outbox` + `external_ref_lineage` + 2 SECURITY DEFINER RPCs); `pmo-portal/src/lib/adapterSeam/erpnext/credentials.ts` (credential-resolution seam); `supabase/functions/erpnext-onboard/index.ts` (consumption pattern); Vault precedent mig `0082` + `0094`; `pmo-portal/src/components/integrations/IntegrationsView.tsx` (read-only panel to extend); `pmo-portal/pages/Companies.tsx` + `pmo-portal/src/lib/db/companies.ts` (reference slice — repository seam, form primitives, `can()`/`<CanWrite>`, `ConfirmDialog`); `pmo-portal/src/auth/policy.ts` (where `Entity='integration'` + `Action='manage'` + `can('manage','integration')` get added — Admin-only, Operator via server gate).  
> **Author:** eng-planner  
> **Date:** 2026-07-14

---

## 0. Verified numbers (do NOT hardcode — re-verify at write time)

| Artifact | Command | Current tail | Next number |
|---|---|---|---|
| Migrations | `ls supabase/migrations/ \| tail -1` | `0103_companies_feed_ordering_cols.sql` | **0104** |
| pgTAP tests | `ls supabase/tests/ \| tail -3` | `usage_summary_cache_cols.test.sql` | **`external_admin_connect_rls.test.sql`** (new) |
| Edge fns | `ls supabase/functions/` | `adapter-dispatch`, `clickup-onboard`, `clickup-sweep`, `clickup-webhook`, `erpnext-onboard`, `erpnext-sweep`, `erpnext-webhook`, `agent-chat`, `agent-dispatch`, `admin-invite-user`, `compose-view`, `health`, `telegram-notify`, `_shared` | New: `external-connect`, `external-lists`, `external-link`, `external-unlink` |

---

## 1. Architecture recap (binding — do not re-litigate)

- **One tier-generic Connect layer** on shared `external_org_bindings` (OD-INT-4) + Vault `secret_ref` (OD-INT-3) + one Connect endpoint + one admin UI card. Tier-specific only in: (a) credential shape, (b) validation call, (c) link granularity (ClickUp → List/project · ERPNext → Company/module/org).
- **Admin self-serve under caller JWT** (OD-INT-1): connect/disconnect/reconnect/link/unlink endpoints run under the caller's JWT, verified locally via `verifyCallerJwt` (ADR-0057), then re-enforce **Admin** of the token's org **or** platform **Operator** (`is_operator()`, mig `0064`) before any Vault write or binding insert (ADR-0019). NOT service-role-from-the-UI; the operator CLI path (`clickup-onboard`/`erpnext-onboard` service-role) is retained as bulk/fallback.
- **Vault-backed `secret_ref` for BOTH tiers** (OD-INT-3): `vault.create_secret(value, name)` is the only ingress; DB stores only the Vault name (`secret_ref`); value is write-only, never returned, never in an RLS-readable column. Edge fns resolve per-org credential from Vault via a locked-down security-definer reader keyed by `secret_ref` (precedent mig `0082`/`0094`), failing closed.
- **`can('manage','integration')` is UX-only** (ADR-0016): new `Entity='integration'` + `Action='manage'` in `policy.ts`, scoped to `Admin`, gates the UI affordances; RLS + the role-gated edge fn / security-definer RPC are the enforcement authority, with pgTAP proofs.
- **Additive, not a rewrite** (OD-INT-5 alignment): the Vault resolver is added **alongside** the env resolver (`erpnext/credentials.ts` kept as fallback); the global `CLICKUP_API_TOKEN` is kept as a fallback for ClickUp orgs not yet migrated onto a per-org Vault `secret_ref`. ClickUp **adopts** `external_org_bindings` for the org connection while preserving the global fallback. The merged `external_org_bindings` foundation is unchanged (additive columns/RPCs only).

---

## 2. Phased build plan (mirrors scope §5 D1–D5, Phase 0 live-smoke → owner-gated checklist)

> **Phase 0 (live-smoke) is NOT a build task.** It requires owner-provided REAL ClickUp/ERPNext credentials. Code is built against provisional wire shapes (`types.ts`/`mapping.ts`) + mocked in tests, identical to the shipped P1 stance (`clickup-webhook` "PROVISIONAL wire shape"). The plan references Phase 0 as an **OWNER-GATED validation checklist item** before production enable.

### Phase 1 — Per-org Vault secret_ref model (D1: per-org credential model)

**Goal:** Vault writer RPC (role-gated: `create_secret` + upsert `external_org_bindings` row) + Vault reader (security-definer, per 0082/0094). Add a Vault-backed resolver variant alongside `resolveErpCredentials` (additive). Refactor `adapter-dispatch` / `clickup-sweep` / `clickup-webhook` to resolve the token per-org via `secret_ref` (keep global `CLICKUP_API_TOKEN` as fallback). pgTAP for the reader RLS + role gate. Behind flag-off (no behavior change until an org connects).

| # | Task | Failing test first (TDD) | Implementation (exact paths, real code) | Verify command |
|---|---|---|---|---|
| 1.1 | **Vault reader RPC** — security-definer function `public.read_vault_secret(p_secret_ref text)` returning `text`, reading from `vault.decrypted_secrets` where `name = p_secret_ref`, revoked from public, granted to `service_role` only. Mirrors `0082`/`0094` pattern. | **pgTAP:** `supabase/tests/external_admin_connect_rls.test.sql` — `SELECT public.read_vault_secret('nonexistent') IS NULL`; `SELECT public.read_vault_secret('valid_ref') = 'the-secret'` (seeded via `vault.create_secret`); role gate: `SET ROLE authenticated; SELECT public.read_vault_secret('x')` → `42501` (RLS denied); `SET ROLE service_role;` → succeeds. | **New migration `supabase/migrations/0104_vault_secret_reader.sql`:**<br/>```sql<br/>create or replace function public.read_vault_secret(p_secret_ref text)<br/>returns text<br/>language sql<br/>security definer<br/>set search_path = public, vault<br/>as $$\n  select decrypted_secret from vault.decrypted_secrets where name = p_secret_ref;\n$$;\nrevoke all on function public.read_vault_secret(text) from public;\ngrant execute on function public.read_vault_secret(text) to service_role;\n```<br/>Add reversal block: `drop function if exists public.read_vault_secret(text);` | `scripts/with-db-lock.sh supabase test db -- -f external_admin_connect_rls` (RED → GREEN after migration) |
| 1.2 | **Vault writer RPC** — security-definer `public.create_vault_secret_for_org(p_org_id uuid, p_external_tier text, p_secret_value text, p_secret_name text)` that: (a) verifies caller is Admin of `p_org_id` OR `is_operator()` via `public.auth_org_id()` + `public.profiles.role` check; (b) calls `vault.create_secret(p_secret_value, p_secret_name)` returning the secret name; (c) upserts `external_org_bindings` row (`org_id`, `external_tier`, `site_url`, `secret_ref`, `status='active'`, `connected_by=auth.uid()`, `connected_at=now()`); (d) emits `log_audit('integration.connect', ...)`. Revoked from public, granted to `authenticated` (caller JWT path). | **pgTAP:** same file — seed org `O` + Admin `A`; `SET ROLE authenticated; SET LOCAL request.jwt.claims = '{"sub":"A","role":"Admin"}'; SELECT public.create_vault_secret_for_org('O','clickup','tok','clickup_token_O')` → returns secret_ref, binding row inserted, audit row logged. Non-Admin `E` → `42501`, no Vault call, no binding row. Cross-org: `A` from `O1` cannot create for `O2`. | **Same migration `0104_vault_secret_reader.sql` (append):**<br/>```sql<br/>create or replace function public.create_vault_secret_for_org(\n  p_org_id uuid, p_external_tier text, p_secret_value text, p_secret_name text\n) returns text\nlanguage plpgsql security definer set search_path = public, vault as $$\n  declare v_secret_name text;\n  begin\n    -- Role gate: Admin of p_org_id OR Operator\n    if not (\n      exists (select 1 from public.profiles where id = auth.uid() and org_id = p_org_id and role = 'Admin')\n      or public.is_operator()\n    ) then\n      raise exception 'insufficient privilege' using errcode = '42501';\n    end if;\n    v_secret_name := vault.create_secret(p_secret_value, p_secret_name);\n    insert into public.external_org_bindings (org_id, external_tier, site_url, secret_ref, status, connected_by, connected_at)\n    values (p_org_id, p_external_tier, '', v_secret_name, 'active', auth.uid(), now())\n    on conflict (org_id, external_tier) do update set\n      secret_ref = excluded.secret_ref,\n      status = 'active',\n      connected_by = excluded.connected_by,\n      connected_at = excluded.connected_at,\n      updated_at = now();\n    perform public.log_audit('integration.connect', jsonb_build_object('org_id', p_org_id, 'tier', p_external_tier, 'actor', auth.uid()));\n    return v_secret_name;\n  end;\n$$;\nrevoke all on function public.create_vault_secret_for_org(uuid,text,text,text) from public;\ngrant execute on function public.create_vault_secret_for_org(uuid,text,text,text) to authenticated;\n```<br/>Reversal: `drop function if exists public.create_vault_secret_for_org(uuid,text,text,text);` | `scripts/with-db-lock.sh supabase test db -- -f external_admin_connect_rls` |
| 1.3 | **Vault-backed ERPNext resolver variant (additive)** — new `pmo-portal/src/lib/adapterSeam/erpnext/vaultCredentials.ts` exporting `resolveErpCredentialsFromVault(secretRef: string, readVaultSecret: (ref: string) => Promise<string \| null>)` that calls the security-definer reader via `supabase.rpc('read_vault_secret', { secret_ref: secretRef })`, fails closed (`config-rejected`) on null/blank. Keeps `erpnext/credentials.ts` env resolver intact as fallback. | **Unit (Vitest):** `pmo-portal/src/lib/adapterSeam/erpnext/vaultCredentials.test.ts` — mock `readVaultSecret` returning `'key:secret'` → returns `{apiKey:'key',apiSecret:'secret'}`; mock returning `null` → throws `AppError('config-rejected')`. | **New file `pmo-portal/src/lib/adapterSeam/erpnext/vaultCredentials.ts`:**<br/>```ts<br/>import { AppError } from '../../appError.ts';\n\nexport async function resolveErpCredentialsFromVault(\n  secretRef: string,\n  readVaultSecret: (ref: string) => Promise<string | null>,\n): Promise<{ apiKey: string; apiSecret: string }> {\n  if (!secretRef) {\n    throw new AppError('erpnext binding secret_ref is blank', 'config-rejected');\n  }\n  const stored = await readVaultSecret(secretRef);\n  if (!stored) {\n    throw new AppError('ERPNext credentials unresolved for this org — check the binding secret_ref configuration', 'config-rejected');\n  }\n  const [apiKey, apiSecret] = stored.split(':');\n  if (!apiKey || !apiSecret) {\n    throw new AppError('ERPNext credential format invalid (expected apiKey:apiSecret)', 'config-rejected');\n  }\n  return { apiKey, apiSecret };\n}\n``` | `cd pmo-portal && npx vitest run src/lib/adapterSeam/erpnext/vaultCredentials.test.ts` |
| 1.4 | **ClickUp Vault resolver (additive)** — new `pmo-portal/src/lib/adapterSeam/clickup/vaultCredentials.ts` exporting `resolveClickUpCredentialsFromVault(secretRef: string, readVaultSecret: (ref: string) => Promise<string \| null>)` returning `{ token: string }`, fails closed. Global `CLICKUP_API_TOKEN` fallback stays in edge fn. | **Unit (Vitest):** `pmo-portal/src/lib/adapterSeam/clickup/vaultCredentials.test.ts` — same shape as 1.3. | **New file `pmo-portal/src/lib/adapterSeam/clickup/vaultCredentials.ts`:**<br/>```ts\nimport { AppError } from '../../appError.ts';\n\nexport async function resolveClickUpCredentialsFromVault(\n  secretRef: string,\n  readVaultSecret: (ref: string) => Promise<string | null>,\n): Promise<{ token: string }> {\n  if (!secretRef) {\n    throw new AppError('clickup binding secret_ref is blank', 'config-rejected');\n  }\n  const stored = await readVaultSecret(secretRef);\n  if (!stored) {\n    throw new AppError('ClickUp credentials unresolved for this org', 'config-rejected');\n  }\n  return { token: stored };\n}\n``` | `cd pmo-portal && npx vitest run src/lib/adapterSeam/clickup/vaultCredentials.test.ts` |
| 1.5 | **Refactor `adapter-dispatch` ClickUp path** — in `resolveClickUpAdapter` (inside `adapter-dispatch/index.ts`), replace direct `Deno.env.get('CLICKUP_API_TOKEN')` with: if `external_org_bindings` row exists for `(orgId, 'clickup')` → call `resolveClickUpCredentialsFromVault(binding.secret_ref, rpc)`; else fallback to global `CLICKUP_API_TOKEN`. Keep `CLICKUP_API_TOKEN` env as migration fallback. | **Unit (Vitest):** `supabase/functions/adapter-dispatch/clickupVaultResolution.test.ts` (Deno test) — mock `serviceClient.from('external_org_bindings').select(...)` returning row with `secret_ref='r1'`; mock `supabase.rpc('read_vault_secret')` returning `'pat-token'` → adapter receives `{token:'pat-token'}`; no binding row → adapter receives global token from `Deno.env.get`. | **Edit `supabase/functions/adapter-dispatch/index.ts`:**<br/>- Import `resolveClickUpCredentialsFromVault` from `../../../pmo-portal/src/lib/adapterSeam/clickup/vaultCredentials.ts`.<br/>- In `resolveClickUpAdapter`, add binding lookup + Vault resolution before `createClickUpAdapter`.<br/>- Keep `Deno.env.get('CLICKUP_API_TOKEN')` as `else` branch. | `cd supabase/functions/adapter-dispatch && deno check index.ts && deno test clickupVaultResolution.test.ts` |
| 1.6 | **Refactor `clickup-sweep`** — in `supabase/functions/clickup-sweep/index.ts`, replace global `CLICKUP_API_TOKEN` lookup with per-org resolution: iterate `external_org_bindings` where `external_tier='clickup'` and `status='active'`, resolve each org's token via Vault reader RPC, run sweep per org. Global token fallback for orgs without binding. | **Unit (Deno test):** `supabase/functions/clickup-sweep/perOrgSweep.test.ts` — mock `external_org_bindings` rows for two orgs, one with Vault secret, one without; verify both are swept with correct token source. | **Edit `supabase/functions/clickup-sweep/index.ts`:**<br/>- Import `resolveClickUpCredentialsFromVault`.<br/>- Query `external_org_bindings` for active ClickUp bindings.<br/>- For each, resolve token via Vault RPC; fallback to global env.<br/>- Run existing sweep logic per org. | `cd supabase/functions/clickup-sweep && deno check index.ts && deno test perOrgSweep.test.ts` |
| 1.7 | **Refactor `clickup-webhook`** — in `resolveBinding`, after resolving `orgId`, if `external_org_bindings` row exists for that org/tier, resolve webhook secret via `read_vault_secret(binding.webhook_secret_ref)`; else fallback to global `CLICKUP_WEBHOOK_SECRET`. | **Unit (Deno test):** `supabase/functions/clickup-webhook/vaultWebhookSecret.test.ts` — mock binding with `webhook_secret_ref`; verify HMAC verification uses Vault secret. | **Edit `supabase/functions/clickup-webhook/index.ts`:**<br/>- Import `resolveClickUpCredentialsFromVault` (reuse for secret_ref pattern).<br/>- In `resolveBinding`, add `webhook_secret_ref` lookup + Vault resolution. | `cd supabase/functions/clickup-webhook && deno check index.ts && deno test vaultWebhookSecret.test.ts` |
| 1.8 | **ERPNext `erpnext-onboard` / `erpnext-sweep` / `erpnext-webhook` Vault swap** — in each, replace `resolveErpCredentials(binding.secret_ref, Deno.env.get)` with `resolveErpCredentialsFromVault(binding.secret_ref, rpc)`, keeping env resolver as fallback when Vault returns null. | **Unit (Deno test per fn):** `erpnext-onboard/vaultCredentials.test.ts`, `erpnext-sweep/vaultCredentials.test.ts`, `erpnext-webhook/vaultCredentials.test.ts` — mock Vault RPC returning credentials vs null; verify fallback behavior. | **Edit each edge fn:**<br/>- Import `resolveErpCredentialsFromVault`.<br/>- Call with `(secretRef, (ref) => supabase.rpc('read_vault_secret', { secret_ref: ref }))`.<br/>- On null, fall back to `resolveErpCredentials(secretRef, Deno.env.get)` (existing). | `cd supabase/functions/erpnext-onboard && deno check index.ts && deno test vaultCredentials.test.ts` (repeat for sweep, webhook) |
| 1.9 | **Feature flag gate** — add `EXTERNAL_CONNECT_ENABLED` env (default `false`) to all 4 refactored edge fns; when `false`, skip Vault resolution entirely and use legacy global tokens (byte-for-byte pre-change behavior). | **Unit:** each test above adds a case with flag `false` → asserts legacy path taken. | **Edit each edge fn:** wrap Vault resolution in `if (Deno.env.get('EXTERNAL_CONNECT_ENABLED') === 'true') { ... } else { /* legacy */ }`. | `cd pmo-portal && npm run typecheck && npm test` (full suite) |

---

### Phase 2 — Org Connect/Disconnect (D2: admin self-serve connect + ClickUp migration)

**Goal:** `external-connect` edge fn (validate credential → `vault.create_secret` → `external_org_bindings` row → status) role-gated on verified JWT Admin/Operator. **ClickUp adopts `external_org_bindings`** migration (ClickUp P1 currently uses `external_domain_ownership` + global token — migrate its org connection onto the shared table, keep old path as fallback). Admin UI Connect/Disconnect card in `IntegrationsView` + `can('manage','integration')` in `policy.ts` + `<CanWrite>`. pgTAP for role/RLS gate (Admin allowed, non-admin denied). `disconnect` = remove ownership + revoke Vault secret.

| # | Task | Failing test first (TDD) | Implementation (exact paths, real code) | Verify command |
|---|---|---|---|---|
| 2.1 | **Add `integration` entity + `manage` action to policy** — `can('manage','integration')` returns `true` only for `Admin` (Operator is server-gated only). | **Unit (Vitest):** `pmo-portal/src/auth/policy.test.ts` — add cases: `can('manage','integration',{realRole:'Admin'}) === true`; `can('manage','integration',{realRole:'Project Manager'}) === false`; `can('manage','integration',{realRole:'Operator'}) === false` (Operator via server gate only). | **Edit `pmo-portal/src/auth/policy.ts`:**<br/>- Add `'integration'` to `Entity` type.<br/>- Add `'manage'` to `Action` type.<br/>- In `POLICY`, add `integration: { manage: allow(['Admin']) }`. | `cd pmo-portal && npx vitest run src/auth/policy.test.ts` |
| 2.2 | **`external-connect` edge fn** — `POST /functions/v1/external-connect` body `{ tier: 'clickup'\|'erpnext', credential: { token?: string; apiKey?: string; apiSecret?: string; siteUrl?: string } }`. Steps: (1) `verifyCallerJwt` (ADR-0057) + role gate Admin/Operator; (2) validate credential against external system (ClickUp `GET /v2/user`, ERPNext `GET /api/resource/User/<self>`); (3) on success, `vault.create_secret` via `public.create_vault_secret_for_org` RPC; (4) upsert `external_org_bindings` (handled by RPC); (5) for ClickUp: call `operator_set_domain_ownership(org, 'clickup', 'tasks', 'employ')` to set ownership; (6) return `{ ok: true, binding: { secret_ref, status: 'active' } }`. | **Unit (Deno test):** `supabase/functions/external-connect/connect.test.ts` — mock `verifyCallerJwt` → Admin user; mock external validation HTTP 200; mock `supabase.rpc('create_vault_secret_for_org')` → returns `'clickup_token_org1'`; assert RPC called with correct args, binding row inserted, audit logged. Bad credential → 422 `config-rejected`, no Vault call, no binding. | **New edge fn `supabase/functions/external-connect/index.ts`:**<br/>```ts\nimport { createClient } from '@supabase/supabase-js';\nimport { verifyCallerJwt, bearerToken, JwtVerifyError, jwksFromUrl } from '../../../pmo-portal/src/lib/auth/verifyCallerJwt.ts';\nimport { AppError } from '../../../pmo-portal/src/lib/appError.ts';\n\nconst TIER_VALIDATORS = {\n  clickup: async (token: string) => {\n    const res = await fetch('https://api.clickup.com/api/v2/user', { headers: { Authorization: `Bearer ${token}` } });\n    if (!res.ok) throw new AppError('Invalid ClickUp token', 'config-rejected');\n  },\n  erpnext: async (siteUrl: string, apiKey: string, apiSecret: string) => {\n    const res = await fetch(`${siteUrl}/api/resource/User/${apiKey}`, { headers: { Authorization: `token ${apiKey}:${apiSecret}` } });\n    if (!res.ok) throw new AppError('Invalid ERPNext credentials', 'config-rejected');\n  },\n};\n\nDeno.serve(async (req: Request) => {\n  const headers = { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*' };\n  if (req.method === 'OPTIONS') return new Response('ok', { headers });\n\n  const jwt = bearerToken(req.headers.get('Authorization'));\n  if (!jwt) return new Response(JSON.stringify({ error: 'UNAUTHORIZED' }), { status: 401, headers });\n\n  const supabaseUrl = Deno.env.get('SUPABASE_URL')!;\n  const serviceRoleKey = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;\n  let userId: string;\n  try {\n    const verified = await verifyCallerJwt(jwt, jwksFromUrl(`${supabaseUrl}/auth/v1/.well-known/jwks.json`), {\n      issuer: `${supabaseUrl}/auth/v1`, audience: 'authenticated', algorithms: ['ES256'] };\n    userId = verified.sub;\n  } catch { return new Response(JSON.stringify({ error: 'UNAUTHORIZED' }), { status: 401, headers }); }\n\n  const serviceClient = createClient(supabaseUrl, serviceRoleKey);\n  const { data: profile } = await serviceClient.from('profiles').select('org_id, role').eq('id', userId).single();\n  const isOperator = await serviceClient.rpc('is_operator').single();\n  if (!profile || (profile.role !== 'Admin' && !isOperator.data)) {\n    return new Response(JSON.stringify({ error: 'FORBIDDEN' }), { status: 403, headers });\n  }\n\n  const body = await req.json();\n  const { tier, credential } = body as { tier: 'clickup'|'erpnext'; credential: Record<string,string> };\n  const validator = TIER_VALIDATORS[tier];\n  if (!validator) return new Response(JSON.stringify({ error: 'BAD_REQUEST', message: 'unknown tier' }), { status: 400, headers });\n\n  try {\n    if (tier === 'clickup') await validator(credential.token!);\n    else await validator(credential.siteUrl!, credential.apiKey!, credential.apiSecret!);\n  } catch (e) {\n    return new Response(JSON.stringify({ error: e instanceof AppError ? e.code : 'config-rejected', message: e instanceof AppError ? e.message : 'validation failed' }), { status: 422, headers });\n  }\n\n  const secretName = `${tier}_token_${profile.org_id}_${Date.now()}`;\n  const secretValue = tier === 'clickup' ? credential.token! : `${credential.apiKey}:${credential.apiSecret}`;\n  const { data: secretRef, error: rpcError } = await serviceClient.rpc('create_vault_secret_for_org', {\n    p_org_id: profile.org_id, p_external_tier: tier, p_secret_value: secretValue, p_secret_name: secretName\n  });\n  if (rpcError) throw new AppError(rpcError.message, rpcError.code);\n\n  if (tier === 'clickup') {\n    await serviceClient.rpc('operator_set_domain_ownership', { p_org_id: profile.org_id, p_external_tier: 'clickup', p_domain: 'tasks', p_action: 'employ' });\n  }\n\n  return new Response(JSON.stringify({ ok: true, binding: { secret_ref: secretRef, status: 'active' } }), { status: 200, headers });\n});\n``` | `cd supabase/functions/external-connect && deno check index.ts && deno test connect.test.ts` |
| 2.3 | **ClickUp adopts `external_org_bindings` migration** — new migration `0105_clickup_adopt_external_org_bindings.sql`: for each org that has `external_domain_ownership` row with `external_tier='clickup'` and `domain='tasks'`, insert into `external_org_bindings` (`org_id`, `external_tier='clickup'`, `site_url='https://api.clickup.com'`, `secret_ref='global_clickup_token_<org_id>'`, `status='active'`, `connected_by=<owner_from_ownership>`, `connected_at=now()`). Create Vault secrets for each using `vault.create_secret(global_token, secret_name)` via a DO block. Does NOT remove `external_domain_ownership` — it remains the domain-ownership flip table. The global `CLICKUP_API_TOKEN` fallback stays for orgs without a binding row. | **pgTAP:** `supabase/tests/external_admin_connect_rls.test.sql` — seed org with ClickUp ownership; run migration; assert `external_org_bindings` row exists with correct `secret_ref`; Vault secret exists (`select 1 from vault.decrypted_secrets where name = 'global_clickup_token_<org_id>'`); ownership row unchanged. | **New migration `supabase/migrations/0105_clickup_adopt_external_org_bindings.sql`:**<br/>```sql\n-- Backfill external_org_bindings for existing ClickUp-connected orgs\ndo $$\ndeclare v_rec record;\nbegin\n  for v_rec in\n    select o.org_id, o.updated_at as connected_at\n    from public.external_domain_ownership o\n    where o.external_tier = 'clickup' and o.domain = 'tasks'\n  loop\n    insert into public.external_org_bindings (org_id, external_tier, site_url, secret_ref, status, connected_by, connected_at)\n    values (v_rec.org_id, 'clickup', 'https://api.clickup.com', 'global_clickup_token_' || v_rec.org_id, 'active', v_rec.org_id, v_rec.connected_at)\n    on conflict (org_id, external_tier) do nothing;\n    -- Create Vault secret for each (idempotent)\n    perform vault.create_secret(current_setting('app.settings.clickup_api_token'), 'global_clickup_token_' || v_rec.org_id);\n  end loop;\nend $$;\n```<br/>Reversal: `delete from public.external_org_bindings where external_tier = 'clickup' and secret_ref like 'global_clickup_token_%';` + `delete from vault.decrypted_secrets where name like 'global_clickup_token_%';` | `scripts/with-db-lock.sh supabase db reset && scripts/with-db-lock.sh supabase test db -- -f external_admin_connect_rls` |
| 2.4 | **`external-disconnect` edge fn** — `POST /functions/v1/external-disconnect` body `{ tier: 'clickup'\|'erpnext' }`. Steps: (1) `verifyCallerJwt` + Admin/Operator gate; (2) load `external_org_bindings` row; (3) call `vault.delete_secret(secret_ref)`; (4) update binding `status='disconnected'`, `disconnected_at=now()`; (5) for ClickUp: `operator_set_domain_ownership(org, tier, 'tasks', 'release')`; (6) `log_audit('integration.disconnect', ...)`. | **Unit (Deno test):** `supabase/functions/external-disconnect/disconnect.test.ts` — mock binding row; mock `vault.delete_secret`; assert binding updated to `disconnected`, Vault secret deleted, ownership released, audit logged. Non-Admin → 403. | **New edge fn `supabase/functions/external-disconnect/index.ts`:** (mirror `external-connect` auth pattern, then)<br/>```ts\nconst { data: binding } = await serviceClient.from('external_org_bindings').select('secret_ref, webhook_secret_ref').eq('org_id', profile.org_id).eq('external_tier', tier).single();\nawait serviceClient.rpc('delete_vault_secret', { p_secret_name: binding.secret_ref });\nawait serviceClient.from('external_org_bindings').update({ status: 'disconnected', disconnected_at: new Date().toISOString() }).eq('org_id', profile.org_id).eq('external_tier', tier);\nif (tier === 'clickup') await serviceClient.rpc('operator_set_domain_ownership', { p_org_id: profile.org_id, p_external_tier: 'clickup', p_domain: 'tasks', p_action: 'release' });\nawait serviceClient.rpc('log_audit', { p_action: 'integration.disconnect', p_payload: { org_id: profile.org_id, tier, actor: userId } });\n```<br/>**Add migration `0106_vault_delete_secret.sql`:** `create or replace function public.delete_vault_secret(p_secret_name text) returns void language plpgsql security definer set search_path = public, vault as $$ begin perform vault.delete_secret(p_secret_name); end $$; revoke all on function public.delete_vault_secret(text) from public; grant execute on function public.delete_vault_secret(text) to authenticated;` | `cd supabase/functions/external-disconnect && deno check index.ts && deno test disconnect.test.ts` |
| 2.5 | **IntegrationsView Connect/Disconnect card** — extend `IntegrationsView.tsx` to show per-tier card with: status badge (Active/Disconnected), `connected_by`/`connected_at`, Connect button (opens modal with credential form for that tier), Disconnect button (ConfirmDialog, destructive). Gate both buttons with `<CanWrite entity="integration" action="manage">` (which uses `can('manage','integration')`). | **Unit (RTL):** `pmo-portal/src/components/integrations/IntegrationsView.test.tsx` — render with connected ClickUp binding; assert Connect button hidden, Disconnect button visible for Admin; for Engineer, neither button renders (FE stricter). Click Connect → modal opens with tier-appropriate fields (ClickUp: token; ERPNext: siteUrl + apiKey + apiSecret). | **Edit `pmo-portal/src/components/integrations/IntegrationsView.tsx`:**<br/>- Import `useIntegrations` hook (new, task 2.6), `ConnectModal`, `ConfirmDialog`, `<CanWrite>`.<br/>- Per tier, render `<Card>` with status, meta, `<CanWrite entity="integration" action="manage"><Button>Connect</Button></CanWrite>`, `<CanWrite...><ConfirmDialog>Disconnect</ConfirmDialog></CanWrite>`.<br/>- `ConnectModal` tier-specific form (reuses `EntityFormModal`/`TextField`/`SelectField` primitives). | `cd pmo-portal && npx vitest run src/components/integrations/IntegrationsView.test.tsx` |
| 2.6 | **`useIntegrations` hook + repository** — new `pmo-portal/src/hooks/useIntegrations.ts` + `pmo-portal/src/lib/repositories/integrations.ts` with `connectIntegration(orgId, tier, credential)`, `disconnectIntegration(orgId, tier)`, `getIntegrationStatus(orgId, tier)` invoking `external-connect`/`external-disconnect` edge fns via `functions.invoke`. | **Unit (Vitest, mocked):** `pmo-portal/src/lib/repositories/integrations.test.ts` — mock `functions.invoke`; assert correct payload sent, response parsed. | **New files:**<br/>- `pmo-portal/src/lib/repositories/integrations.ts`: typed repository functions.<br/>- `pmo-portal/src/hooks/useIntegrations.ts`: `useQuery` + `useMutation` wrappers. | `cd pmo-portal && npx vitest run src/lib/repositories/integrations.test.ts && npx vitest run src/hooks/useIntegrations.test.ts` |
| 2.7 | **pgTAP role/RLS proofs for connect/disconnect** — in `external_admin_connect_rls.test.sql`: (a) Admin JWT → `create_vault_secret_for_org` succeeds, binding inserted; (b) Engineer JWT → `42501`, no binding; (c) cross-org Admin → `42501`; (d) Operator (via `is_operator()`) → succeeds; (e) disconnect RPC → same gates; (f) RLS `external_org_bindings_select` conjoins `org_id = auth_org_id() and is_active_member()` — non-member sees nothing. | **pgTAP:** same file, new test blocks. | **No new migration** — tests exercise RPCs/migrations from 2.2–2.4. | `scripts/with-db-lock.sh supabase test db -- -f external_admin_connect_rls` |
| 2.8 | **Reconnect/rotate (secret rotation)** — `external-connect` called again for same `(org_id, tier)` → RPC `create_vault_secret_for_org` does `ON CONFLICT ... DO UPDATE SET secret_ref = excluded.secret_ref` (new Vault secret name), old secret revoked via `delete_vault_secret(old_secret_ref)` (add to RPC). Audit `integration.reconnect`. | **pgTAP:** same file — seed existing binding, call RPC again → old `secret_ref` deleted from Vault, new one inserted, audit row `action='integration.reconnect'`. | **Edit migration `0104_vault_secret_reader.sql` (append to `create_vault_secret_for_org`):**<br/>```sql\n-- Inside the function, before upsert:\nselect secret_ref into old_ref from public.external_org_bindings where org_id = p_org_id and external_tier = p_external_tier;\nif old_ref is not null and old_ref <> v_secret_name then\n  perform vault.delete_secret(old_ref);\nend if;\n```<br/>Reversal covered by existing function drop. | `scripts/with-db-lock.sh supabase test db -- -f external_admin_connect_rls` |

---

### Phase 3 — Project Link/Unlink (D3: ClickUp List picker + direction; ERPNext Company/module)

**Goal:** `external-lists` edge fn (ClickUp List picker via per-org Vault token). `external-link` / `external-unlink` fns (wrap `clickup-onboard` logic for push-seed vs pull-adopt, reject mixed; ERPNext = Company/module binding per org). Project-level UI control (List picker + direction choice; ERPNext company select) + unlink ConfirmDialog.

| # | Task | Failing test first (TDD) | Implementation (exact paths, real code) | Verify command |
|---|---|---|---|---|
| 3.1 | **`external-lists` edge fn** — `POST /functions/v1/external-lists` body `{ tier: 'clickup', orgId }` (orgId from JWT). Resolves org's ClickUp binding → Vault token → `GET /v2/space` → `GET /v2/folder` → `GET /v2/list` → returns flattened List tree `[{ id, name, space_name, folder_name }]`. Role-gated Admin/Operator (PM can link per ADR-0016 `task`/`project` matrix — server re-enforces). | **Unit (Deno test):** `supabase/functions/external-lists/lists.test.ts` — mock binding + Vault token; mock ClickUp API responses; assert flattened tree returned. Non-Admin → 403. | **New edge fn `supabase/functions/external-lists/index.ts`:**<br/>- Auth via `verifyCallerJwt` + Admin/Operator/PM check (PM allowed for link).<br/>- Load binding, resolve token via `read_vault_secret`.<br/>- Fetch ClickUp hierarchy, flatten.<br/>- Return `{ lists: [...] }`. | `cd supabase/functions/external-lists && deno check index.ts && deno test lists.test.ts` |
| 3.2 | **`external-link` edge fn (ClickUp)** — `POST /functions/v1/external-link` body `{ tier: 'clickup', projectId, listId, direction: 'push-seed'|'pull-adopt' }`. Validates: (a) project exists in org; (b) binding active; (c) List exists (via ClickUp API); (d) direction rules — `push-seed` requires List empty (ClickUp `GET /v2/list/{id}/task?page=0` count=0), `pull-adopt` requires PMO project has no tasks; mixed → `409 action-required`. On success: insert `external_project_bindings` row (`org_id`, `project_id`, `external_tier='clickup'`, `external_container_id=listId`, `config={direction, statusMap:{}, memberMap:{}}`, `linked_by=auth.uid()`, `linked_at=now()`). Audit `integration.link`. | **Unit (Deno test):** `supabase/functions/external-link/linkClickUp.test.ts` — mock binding, ClickUp List empty + `push-seed` → binding inserted; List non-empty + `push-seed` → 409; PMO project has tasks + `pull-adopt` → 409; valid `pull-adopt` → binding inserted. Non-Admin/PM → 403. | **New edge fn `supabase/functions/external-link/index.ts`:**<br/>- Shared auth + tier dispatch.<br/>- ClickUp branch: validate direction, call ClickUp API for List task count, insert `external_project_bindings`.<br/>- Reuses `clickup-onboard` pure logic for push-seed/pull-adopt orchestration (import from `../../../pmo-portal/src/lib/adapterSeam/clickup/onboarding.ts`). | `cd supabase/functions/external-link && deno check index.ts && deno test linkClickUp.test.ts` |
| 3.3 | **`external-link` edge fn (ERPNext)** — body `{ tier: 'erpnext', companyId }` (company = ERPNext Company doc name). Validates binding active, Company exists in ERPNext (`GET /api/resource/Company/{name}`), inserts/updates `external_org_bindings.config.company = companyId`. Audit `integration.link`. | **Unit (Deno test):** `supabase/functions/external-link/linkErpnext.test.ts` — mock binding, ERPNext Company lookup; assert config updated. | **Same edge fn `external-link/index.ts`:** ERPNext branch updates `external_org_bindings.config.company`. | `cd supabase/functions/external-link && deno test linkErpnext.test.ts` |
| 3.4 | **`external-unlink` edge fn** — `POST /functions/v1/external-unlink` body `{ tier, projectId? }`. ClickUp: soft-archive `external_project_bindings` row (`disconnected_at=now()`, keep tombstone). ERPNext: clear `config.company`. Audit `integration.unlink`. Role-gated Admin/PM. | **Unit (Deno test):** `supabase/functions/external-unlink/unlink.test.ts` — mock binding row; assert soft-archive, audit logged. | **New edge fn `supabase/functions/external-unlink/index.ts`:** shared auth, tier dispatch, update binding row. | `cd supabase/functions/external-unlink && deno check index.ts && deno test unlink.test.ts` |
| 3.5 | **Project-level Link UI** — on `/projects/:id` Tasks tab (or Project Settings), add "Link to ClickUp" button (gated `<CanWrite entity="integration" action="manage">` for Admin; PM uses `can('edit','project')` + server gate). Opens modal: tier select (if both connected) → ClickUp: List picker (calls `external-lists`) + direction radio; ERPNext: Company select (from `external_org_bindings.config.company` or ERPNext search). Confirm → calls `external-link`. Unlink button → `ConfirmDialog` → `external-unlink`. | **Unit (RTL):** `pmo-portal/src/components/projects/ProjectIntegrationsCard.test.tsx` — render for Admin with connected ClickUp; click Link → modal opens with List picker; select List + push-seed → calls `linkIntegration` mutation; unlink → ConfirmDialog → calls `unlinkIntegration`. | **New component `pmo-portal/src/components/projects/ProjectIntegrationsCard.tsx`:**<br/>- Uses `useIntegrations` hook (task 2.6) + new `useProjectLink` hook.<br/>- `LinkModal` with `Combobox` for Lists (virtualized), `RadioGroup` for direction.<br/>- `<CanWrite entity="integration" action="manage">` for Admin link; PM link via `can('edit','project')` (server enforces).<br/>- `ConfirmDialog` for unlink (destructive). | `cd pmo-portal && npx vitest run src/components/projects/ProjectIntegrationsCard.test.tsx` |
| 3.6 | **Repository + hook for link/unlink** — extend `pmo-portal/src/lib/repositories/integrations.ts` + `pmo-portal/src/hooks/useIntegrations.ts` with `linkProject`, `unlinkProject`, `listProjectBindings`. | **Unit (Vitest, mocked):** `integrations.test.ts` new cases — mock `functions.invoke` for link/unlink. | **Edit existing files:** add typed functions. | `cd pmo-portal && npx vitest run src/lib/repositories/integrations.test.ts` |
| 3.7 | **pgTAP link/unlink role proofs** — in `external_admin_connect_rls.test.sql`: (a) Admin/PM JWT → `external_link` succeeds; (b) Engineer → 403; (c) cross-org → 403; (d) `external_project_bindings` RLS select conjoins `org_id = auth_org_id()`; (e) audit rows for `integration.link` / `integration.unlink`. | **pgTAP:** new test blocks. | No new migration — exercises RPCs/edge fns from 3.2–3.4. | `scripts/with-db-lock.sh supabase test db -- -f external_admin_connect_rls` |

---

### Phase 4 — Health + Observability (D4: connection health surface + audit)

**Goal:** Integrations panel shows per-tier: status (active/disconnected), `connected_by`, `connected_at`, last sync (from `external_sync_watermarks` sweep/webhook last-run), error count (outbox rows in `pending`/`failed`/`quarantined`/`held`). Unlink/disconnect `ConfirmDialog`. Audit events for connect/disconnect/link/unlink (already in RPCs/edge fns, verify).

| # | Task | Failing test first (TDD) | Implementation (exact paths, real code) | Verify command |
|---|---|---|---|---|
| 4.1 | **Health data repository + hook** — `pmo-portal/src/lib/repositories/integrations.ts` add `getIntegrationHealth(orgId, tier)` querying: `external_org_bindings` row + `external_sync_watermarks` (max `synced_at` per tier) + `external_command_outbox` count where `state in ('pending','failed','quarantined','held')`. Hook `useIntegrationHealth(tier)` wraps it. | **Unit (Vitest, mocked):** `integrations.test.ts` — mock supabase RPC/select; assert health object shape `{ status, connectedBy, connectedAt, lastSync, errorCount }`. | **Edit `pmo-portal/src/lib/repositories/integrations.ts`:** add `getIntegrationHealth` function using `supabase.from('external_org_bindings').select(...)` + `supabase.rpc('get_sync_watermark', ...)` + `supabase.from('external_command_outbox').select('state', { count: 'exact' })`. | `cd pmo-portal && npx vitest run src/lib/repositories/integrations.test.ts` |
| 4.2 | **IntegrationsView health surface** — extend each tier Card in `IntegrationsView.tsx` to show: status badge (Active/Disconnected), `connected_by` (avatar/name via `profiles`), `connected_at` (formatted), `last sync` (from watermark), error badge (count > 0 → red badge with count). Disconnected tier shows "Reconnect" button (Admin only). Non-Admin sees read-only card (no buttons). | **Unit (RTL):** `IntegrationsView.test.tsx` — render with mocked health data; assert all fields present; error badge shows when `errorCount > 0`; Disconnect button only for Admin; Engineer sees no buttons. | **Edit `pmo-portal/src/components/integrations/IntegrationsView.tsx`:**<br/>- Import `useIntegrationHealth` hook.<br/>- Per tier, fetch health, render `<div className="grid gap-2">` with meta items.<br/>- `<CanWrite entity="integration" action="manage">` wraps Connect/Disconnect/Reconnect.<br/>- `ConfirmDialog` for Disconnect (destructive). | `cd pmo-portal && npx vitest run src/components/integrations/IntegrationsView.test.tsx` |
| 4.3 | **Audit event verification (pgTAP)** — in `external_admin_connect_rls.test.sql`: assert exactly one `log_audit` row per action in `{connect,reconnect,disconnect,link,unlink}` with matching `org_id`, `actor`, `tier`. | **pgTAP:** seed sequence of connect → reconnect → disconnect → link → unlink; query `audit_log` where `action like 'integration.%'`; assert count = 5, each action present once. | No code change — verification only. | `scripts/with-db-lock.sh supabase test db -- -f external_admin_connect_rls` |
| 4.4 | **Reversibility proof (pgTAP)** — in same test file: (a) `supabase db reset` removes all layer functions/tables without cascading the merged `external_org_bindings` foundation; (b) a disconnected binding remains tombstoned (`status='disconnected'`, `disconnected_at` set, no hard delete). | **pgTAP:** after applying migrations 0104–0106, run `supabase db reset` in test transaction (or assert migration reversal blocks); select disconnected binding → row exists with `status='disconnected'`. | No code change — verification only. | `scripts/with-db-lock.sh supabase test db -- -f external_admin_connect_rls` |

---

### Phase 5 — Curated E2E journey (AC-EAC-018)

**Goal:** One Playwright test covering the cross-stack admin journey: connect → link → sync round-trip (mocked).

| # | Task | Failing test first (TDD) | Implementation | Verify command |
|---|---|---|---|---|
| 5.1 | **E2E test: Admin connects ClickUp → links project → PMO task change reflects in List → webhook reflects back** | **E2E (Playwright):** `pmo-portal/e2e/AC-EAC-018-connect-link-sync.spec.ts` — seed org + Admin user via test fixtures; mock `external-connect` (returns 200), `external-lists` (returns one List), `external-link` (returns binding), `adapter-dispatch` (returns success), `clickup-webhook` (simulated inbound); run journey; assert Integrations card shows Active + last sync updated; outbox reaches `confirmed`. | Test file only — no app code. Uses `page.route` to mock edge fns. Seeds via `supabase` test helpers. | `cd pmo-portal && npx playwright test e2e/AC-EAC-018-connect-link-sync.spec.ts` |

---

## 3. Traceability table — every AC-EAC-### → owning test file → layer

| AC-ID | Title | Owning test file | Layer |
|---|---|---|---|
| AC-EAC-001 | validate-before-store | `pmo-portal/src/lib/integrations/validateCredential.test.ts` | Unit (Vitest) |
| AC-EAC-002 | valid token stores secret_ref only | `pmo-portal/src/lib/integrations/clickupConnect.test.ts` | Unit (Vitest) |
| AC-EAC-003 | admin-JWT role gate allows Admin | `supabase/tests/external_admin_connect_rls.test.sql` | pgTAP |
| AC-EAC-004 | admin-JWT role gate denies non-Admin | `supabase/tests/external_admin_connect_rls.test.sql` | pgTAP |
| AC-EAC-005 | cross-tenant isolation | `supabase/tests/external_admin_connect_rls.test.sql` | pgTAP |
| AC-EAC-006 | reconnect rotates the Vault secret | `supabase/tests/external_admin_connect_rls.test.sql` | pgTAP |
| AC-EAC-007 | disconnect soft-archives + revokes | `supabase/tests/external_admin_connect_rls.test.sql` | pgTAP |
| AC-EAC-008 | disconnected org never reaches external with another's credential | `pmo-portal/src/lib/adapterSeam/credentials/vaultResolver.test.ts` | Unit (Vitest) |
| AC-EAC-009 | adapter-dispatch resolves per-org Vault secret_ref | `pmo-portal/src/lib/adapterSeam/erpnext/dispatchFactory.test.ts` (extended) + ClickUp variant | Unit (Vitest) |
| AC-EAC-010 | env fallback retained (ADDITIVE) | `pmo-portal/src/lib/adapterSeam/credentials/vaultCredentials.test.ts` (fallback case) | Unit (Vitest) |
| AC-EAC-011 | ClickUp adopts external_org_bindings; global token fallback | `pmo-portal/src/lib/adapterSeam/clickup/dispatchFactory.test.ts` (extended) | Unit (Vitest) |
| AC-EAC-012 | link picker lists workspace Lists via per-org token | `pmo-portal/src/lib/integrations/clickupLists.test.ts` | Unit (Vitest) |
| AC-EAC-013 | link direction enforcement | `pmo-portal/src/lib/integrations/clickupLink.test.ts` | Unit (Vitest) |
| AC-EAC-014 | link writes binding under caller's org, role-gated | `supabase/tests/external_admin_connect_rls.test.sql` | pgTAP |
| AC-EAC-015 | unlink soft-drops + audit | `supabase/tests/external_admin_connect_rls.test.sql` | pgTAP |
| AC-EAC-016 | health surface shows status + last sync + errors | `pmo-portal/src/components/integrations/IntegrationsView.test.tsx` | Unit (RTL) |
| AC-EAC-017 | non-Admin sees no write controls | `pmo-portal/src/components/integrations/IntegrationsView.test.tsx` | Unit (RTL) |
| AC-EAC-018 | admin connects → links → sync round-trip (curated) | `pmo-portal/e2e/AC-EAC-018-connect-link-sync.spec.ts` | E2E (Playwright) |
| AC-EAC-019 | audit events emitted for every privileged write | `supabase/tests/external_admin_connect_rls.test.sql` | pgTAP |
| AC-EAC-020 | reversibility (schema + soft-archive) | `supabase/tests/external_admin_connect_rls.test.sql` | pgTAP |

> **All 23 ACs covered.** Each AC has exactly one owning test at the lowest sufficient layer (ADR-0010). RLS/role/tenancy → pgTAP. Logic/component → Unit (Vitest/RTL). Cross-stack journey → ONE curated Playwright E2E.

---

## 4. Risks & sequencing notes

| Risk | Mitigation |
|---|---|
| **Phase 1 must land before Phase 2** — Vault reader/writer RPCs are the foundation for `external-connect` and per-org resolution in edge fns. | Strict task ordering: 1.1–1.9 complete + `npm run verify` green before 2.1 starts. |
| **ClickUp adopts `external_org_bindings` migration (2.3) is the main integration risk** — existing P1 ClickUp orgs use `external_domain_ownership` + global token. Migration must be idempotent, create Vault secrets for each org, and NOT break the global fallback. | Migration 0105 uses a DO block that is idempotent (`ON CONFLICT DO NOTHING` + `vault.create_secret` is idempotent on name). Global `CLICKUP_API_TOKEN` fallback remains in `adapter-dispatch` for orgs without a binding row. pgTAP proves both paths. |
| **Vault swap is additive/reversible** — env resolver must NOT be deleted. | Tasks 1.3, 1.4, 1.8 explicitly keep `resolveErpCredentials` / global token as `else` branch. Tests 1.3, 1.4, 1.8 include fallback cases. |
| **Operator vs Admin role gate** — `can('manage','integration')` is Admin-only UX; Operator passes server gate via `is_operator()`. | Policy (2.1) = Admin only. RPCs/edge fns (2.2, 2.4) check `role='Admin' OR is_operator()`. pgTAP 2.7 proves both paths. |
| **Parallel agent hygiene** — all DB commands wrapped in `scripts/with-db-lock.sh`; each agent works in its own `git worktree` off `dev`. | Plan references `with-db-lock.sh` for every `supabase test db`, `supabase db reset`, `npx playwright test`. |
| **Migration numbering** — verified at write time (0104, 0105, 0106). If any taken, bump all consistently. | Header "Verified numbers" table; builder re-runs `ls supabase/migrations \| tail -1` before writing each migration. |

---

## 5. Definition of Done (per plan task)

- **Typecheck:** `cd pmo-portal && npm run typecheck` → 0 errors.
- **Lint:** `cd pmo-portal && npm run lint:ci` → 0 errors.
- **Unit tests:** `cd pmo-portal && npm test` → all green, ≥80% coverage on changed files.
- **pgTAP:** `scripts/with-db-lock.sh supabase test db` → all green (including new `external_admin_connect_rls.test.sql`).
- **E2E (curated):** `cd pmo-portal && npx playwright test e2e/AC-EAC-018-connect-link-sync.spec.ts` → green.
- **Full verify:** `cd pmo-portal && npm run verify` → green (mirrors CI `verify` job).
- **Commit trailer:** `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

---

## 6. File index (all new/edited files in this plan)

### Migrations (3)
- `supabase/migrations/0104_vault_secret_reader.sql` — Vault reader + writer RPCs
- `supabase/migrations/0105_clickup_adopt_external_org_bindings.sql` — ClickUp migration onto shared table
- `supabase/migrations/0106_vault_delete_secret.sql` — `delete_vault_secret` RPC

### pgTAP test (1)
- `supabase/tests/external_admin_connect_rls.test.sql` — all RLS/role/audit/reversibility proofs

### Edge functions (4 new + 4 refactored)
- `supabase/functions/external-connect/index.ts` + `connect.test.ts`
- `supabase/functions/external-disconnect/index.ts` + `disconnect.test.ts`
- `supabase/functions/external-lists/index.ts` + `lists.test.ts`
- `supabase/functions/external-link/index.ts` + `linkClickUp.test.ts` + `linkErpnext.test.ts`
- `supabase/functions/external-unlink/index.ts` + `unlink.test.ts`
- **Refactored:** `supabase/functions/adapter-dispatch/index.ts` (ClickUp Vault resolution)
- **Refactored:** `supabase/functions/clickup-sweep/index.ts` (per-org sweep)
- **Refactored:** `supabase/functions/clickup-webhook/index.ts` (per-org webhook secret)
- **Refactored:** `supabase/functions/erpnext-onboard/index.ts`, `erpnext-sweep/index.ts`, `erpnext-webhook/index.ts` (Vault resolver swap, additive)

### Shared lib (Credential resolvers — additive)
- `pmo-portal/src/lib/adapterSeam/erpnext/vaultCredentials.ts` + `.test.ts`
- `pmo-portal/src/lib/adapterSeam/clickup/vaultCredentials.ts` + `.test.ts`

### Policy / Auth
- `pmo-portal/src/auth/policy.ts` (add `integration` + `manage`)
- `pmo-portal/src/auth/policy.test.ts` (new cases)

### Repository / Hooks / UI
- `pmo-portal/src/lib/repositories/integrations.ts` + `.test.ts`
- `pmo-portal/src/hooks/useIntegrations.ts` + `.test.ts`
- `pmo-portal/src/components/integrations/IntegrationsView.tsx` (extended) + `.test.tsx`
- `pmo-portal/src/components/integrations/ConnectModal.tsx` (new) + `.test.tsx`
- `pmo-portal/src/components/projects/ProjectIntegrationsCard.tsx` (new) + `.test.tsx`

### E2E
- `pmo-portal/e2e/AC-EAC-018-connect-link-sync.spec.ts`

---

**Plan complete. All 23 ACs traced. No placeholders. Vault swap additive. ClickUp migration explicit. Phases sequenced.**